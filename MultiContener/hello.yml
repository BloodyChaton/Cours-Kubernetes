apiVersion: v1
kind: Pod
metadata:
  name: hello-pod
  namespace: apprdev
spec:
  containers:
  - name: nginx
    image: bloodychaton/tpmulti:multinginx
    # resources:
    #   requests:
    #     memory: "100Mi"
    #     cpu: "1"
    #   limits:
    #     memory: "200Mi"
    #     cpu: "1"
    ports:
    - containerPort: 80
    command: ["nginx"]
    args: ["-g", "daemon off;", "-q"]  
  - name: appgo
    image: bloodychaton/tpmulti:multiappgo
    # resources:
    #   requests:
    #     memory: "100Mi"
    #     cpu: "1"
    #   limits:
    #     memory: "200Mi"
    #     cpu: "1"
    ports:
    - containerPort: 8080
  imagePullSecrets:
    - name: regcred
